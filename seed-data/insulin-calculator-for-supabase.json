{
  "name": "Insulin Calculator",
  "description": "Calculate insulin dosage using the formula ((x * 18) - 80) / 40",
  "tags": ["health", "calculator", "insulin", "diabetes"],
  "triggerPhrases": [
    "insul",
    "insulin",
    "calculate insulin",
    "insulin dose",
    "insulin dosage"
  ],
  "parameters": [
    {
      "name": "x",
      "type": "number",
      "required": true,
      "prompt": "What is the value for the calculation?"
    }
  ],
  "executionType": "local",
  "code": "const result = ((x * 18) - 80) / 40;\nconst rounded = Math.round(result * 100) / 100;\nreturn `For a value of ${x}, the calculated result is: ${rounded}`;"
}

